<reactiveui:ReactiveUserControl
    x:Class="v2rayN.Views.MsgView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:reactiveui="http://reactiveui.net"
    xmlns:resx="clr-namespace:ServiceLib.Resx;assembly=ServiceLib"
    xmlns:vms="clr-namespace:ServiceLib.ViewModels;assembly=ServiceLib"
    d:DesignHeight="450"
    d:DesignWidth="800"
    x:TypeArguments="vms:MsgViewModel"
    Style="{StaticResource ViewGlobal}"
    mc:Ignorable="d">
    <DockPanel Margin="{StaticResource Margin4}">
        <WrapPanel
            Margin="{StaticResource Margin8}"
            VerticalAlignment="Center"
            DockPanel.Dock="Top"
            Orientation="Horizontal">

            <ComboBox
                x:Name="cmbMsgFilter"
                Width="200"
                Margin="{StaticResource MarginLeftRight8}"
                materialDesign:HintAssist.Hint="{DynamicResource String_MsgFilterTitle}"
                materialDesign:TextFieldAssist.HasClearButton="True"
                AutomationProperties.Name="{DynamicResource String_MsgFilterTitle}"
                IsEditable="True"
                Style="{StaticResource DefComboBox}" />
            <Button
                x:Name="btnCopy"
                Width="24"
                Height="24"
                Margin="{StaticResource MarginLeftRight8}"
                AutomationProperties.Name="{DynamicResource String_menuMsgViewCopyAll}"
                Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}"
                ToolTip="{DynamicResource String_menuMsgViewCopyAll}">
                <materialDesign:PackIcon VerticalAlignment="Center" Kind="ContentCopy" />
            </Button>
            <Button
                x:Name="btnClear"
                Width="24"
                Height="24"
                Margin="{StaticResource MarginLeftRight8}"
                AutomationProperties.Name="{DynamicResource String_menuMsgViewClear}"
                Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}"
                ToolTip="{DynamicResource String_menuMsgViewClear}">
                <materialDesign:PackIcon VerticalAlignment="Center" Kind="Delete" />
            </Button>
            <TextBlock
                Margin="{StaticResource MarginLeftRight8}"
                VerticalAlignment="Center"
                Style="{StaticResource ToolbarTextBlock}"
                Text="{DynamicResource String_TbAutoRefresh}" />
            <ToggleButton
                x:Name="togAutoRefresh"
                Margin="{StaticResource MarginLeftRight8}"
                HorizontalAlignment="Left"
                AutomationProperties.Name="{DynamicResource String_TbAutoRefresh}"
                IsChecked="True" />
            <TextBlock
                Margin="{StaticResource MarginLeftRight8}"
                VerticalAlignment="Center"
                Style="{StaticResource ToolbarTextBlock}"
                Text="{DynamicResource String_TbAutoScrollToEnd}" />
            <ToggleButton
                x:Name="togScrollToEnd"
                Margin="{StaticResource MarginLeftRight8}"
                HorizontalAlignment="Left"
                AutomationProperties.Name="{DynamicResource String_TbAutoScrollToEnd}"
                IsChecked="True" />
            <TextBlock
                Margin="{StaticResource MarginLeftRight8}"
                VerticalAlignment="Center"
                Style="{StaticResource ToolbarTextBlock}"
                Text="{DynamicResource String_TbSettingsShowTechnicalDetails}" />
            <ToggleButton
                x:Name="togProfessionalMode"
                Margin="{StaticResource MarginLeftRight8}"
                HorizontalAlignment="Left"
                AutomationProperties.Name="{DynamicResource String_TbSettingsShowTechnicalDetails}"
                IsChecked="False" />
        </WrapPanel>
        <TextBox
            Name="txtMsg"
            VerticalAlignment="Stretch"
            AcceptsReturn="True"
            BorderThickness="0"
            FontSize="{DynamicResource StdFontSize-1}"
            HorizontalScrollBarVisibility="Auto"
            IsReadOnly="True"
            IsReadOnlyCaretVisible="True"
            IsUndoEnabled="False"
            TextAlignment="Left"
            TextWrapping="Wrap"
            UndoLimit="0"
            VerticalScrollBarVisibility="Visible">
            <TextBox.ContextMenu>
                <ContextMenu Style="{StaticResource DefContextMenu}">
                    <MenuItem
                        x:Name="menuMsgViewSelectAll"
                        Height="{StaticResource MenuItemHeight}"
                        Header="{DynamicResource String_menuMsgViewSelectAll}"
                        InputGestureText="Ctrl+A" />
                    <MenuItem
                        x:Name="menuMsgViewCopy"
                        Height="{StaticResource MenuItemHeight}"
                        Header="{DynamicResource String_menuMsgViewCopy}"
                        InputGestureText="Ctrl+C" />
                    <MenuItem
                        x:Name="menuMsgViewCopyAll"
                        Height="{StaticResource MenuItemHeight}"
                        Header="{DynamicResource String_menuMsgViewCopyAll}" />
                    <MenuItem
                        x:Name="menuMsgViewClear"
                        Height="{StaticResource MenuItemHeight}"
                        Header="{DynamicResource String_menuMsgViewClear}" />
                </ContextMenu>
            </TextBox.ContextMenu>
        </TextBox>
    </DockPanel>
</reactiveui:ReactiveUserControl>
